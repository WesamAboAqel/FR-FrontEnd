export interface AuthorizedPerson {
    id: number; // Unique identifier for the authorized person
    name: string; // Name of the authorized person
    image?: string; // Optional URL for the person's image
    // Add any other properties as needed
} 