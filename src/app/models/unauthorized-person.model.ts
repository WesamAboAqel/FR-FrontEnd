export interface UnauthorizedPerson {
    id: number; // Unique identifier for the unauthorized person
    name: string; // Name of the unauthorized person
    image?: string; // Optional URL for the person's image
    // Add any other properties as needed
} 