export interface FlaggedPerson {
    id: number; // Unique identifier for the flagged person
    name: string; // Name of the flagged person
    image?: string; // Optional URL for the person's image
    // Add any other properties as needed
} 